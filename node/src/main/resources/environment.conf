node {
  data {
    directory = ${?NODE_APPLICATION_DATA_DIR}
  }
  staking {
    directory = ${?NODE_APPLICATION_STAKING_DIR}
  }
  p2p {
    bind-host = ${?NODE_P2P_BIND_HOST}
    bind-port = ${?NODE_P2P_BIND_PORT}
    public-host = ${?NODE_P2P_PUBLIC_HOST}
    public-port = ${?NODE_P2P_PUBLIC_PORT}
    known-peers = ${?NODE_P2P_KNOWN_PEERS}
  }
  rpc {
    bind-host = ${?NODE_RPC_BIND_HOST}
    bind-port = ${?NODE_RPC_BIND_PORT}
  }
}
